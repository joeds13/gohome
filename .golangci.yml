version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - bodyclose
    - errcheck
    - govet
    - ineffassign
    - misspell
    - noctx
    - staticcheck
    - unused

issues:
  exclude-dirs:
    - vendor
    - bin
    - dist
  exclude-files:
    - ".*\\.pb\\.go$"
